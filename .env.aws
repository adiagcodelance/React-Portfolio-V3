# AWS Deployment Environment Variables
# Copy this to .env.production and fill in your values

# Frontend Deployment (S3)
S3_BUCKET_NAME=your-portfolio-bucket-name
AWS_REGION=us-east-1
CLOUDFRONT_DISTRIBUTION_ID=your-cloudfront-distribution-id

# Backend API (Lambda + RDS)
FRONTEND_URL=https://your-domain.com
JWT_SECRET=your-very-long-secure-jwt-secret-key-here
DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/portfoliodb
DB_USERNAME=portfoliouser
DB_PASSWORD=your-secure-db-password

# S3 Uploads Bucket
UPLOADS_BUCKET=your-portfolio-bucket-uploads

# AWS Credentials (if not using AWS CLI profile)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key